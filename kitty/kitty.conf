# Kitty config ported from KDE Konsole (Profile 1).
# Symlinked to ~/.config/kitty/kitty.conf by ./sync. See CLAUDE.md for the mapping.
#
# The palette below was captured from the running Konsole via OSC escape queries
# (OSC 10 = foreground, OSC 11 = background, OSC 4 = ANSI colors 0-15) — i.e. the
# colors Konsole ACTUALLY renders. Profile 1 sets no color scheme, so Konsole
# uses its built-in default 16-color table (the classic 0xB21818-style ANSI
# colors) over a dark #232627 background. NOTE: this is *not* the saturated
# Breeze.colorscheme file in ~/.local/share/konsole/ — that file was never
# selected by the profile and is unused.

# --- Font (Konsole Profile 1: Hack, 11pt) ---
font_family      Hack
font_size        11.0

# --- Window: remember last size, mirroring Konsole's per-screen geometry ---
remember_window_size   yes

# --- Tab bar (Konsole TabBarPosition=Bottom) ---
tab_bar_edge     bottom
# Flat tabs split by a thin separator, matching Konsole's understated bar.
tab_bar_style    separator
# Title = the foreground program name (bash, nvim, ssh), like Konsole's tabs.
# {tab.active_exe} is the basename of the active window's process; this overrides
# the shell-integration title (which would show cwd at the prompt) so the tab is
# always the running command. NOTE: the bar only appears with >=2 tabs
# (tab_bar_min_tabs defaults to 2); open one with ctrl+shift+t.
tab_title_template "{index}: {tab.active_exe}"

# Tab navigation on shift+arrows (kitty's ctrl+shift+arrows still work too).
map shift+right  next_tab
map shift+left   previous_tab

# --- Background transparency (to match Konsole's translucent window) ---
background_opacity   0.7
background_blur      0

# --- Colors: captured from Konsole via OSC query (exact match) ---
foreground   #fcfcfc
background   #232627

# normal
color0   #000000
color1   #b21818
color2   #18b218
color3   #b26818
color4   #1818b2
color5   #b218b2
color6   #18b2b2
color7   #b2b2b2
# bright
color8   #686868
color9   #ff5454
color10  #54ff54
color11  #ffff54
color12  #5454ff
color13  #ff54ff
color14  #54ffff
color15  #ffffff
